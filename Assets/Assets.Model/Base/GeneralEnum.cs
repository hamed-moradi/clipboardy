namespace Assets.Model.Base {

    public enum Status {
        Inactive = 0,
        Active = 10,
        Pending = 20,
        Deleted = 30
    }

    public enum AccountProvider {
        Clipboard = 1,
        Google = 2
    }
}
